/.quarto/
# Per-doc support dirs (<doc>_files/) left behind by rendering; the website uses
# site_libs/ instead and no HTML references these, so they are unreferenced
# build noise (appears at the repo root and under _site/).
*_files/
**/*.quarto_ipynb
.DS_Store
**/.DS_Store
